name: Third Workflow
on: [push]

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display some text
        run: echo "Same text"
      - name: Command in Multiline 
        run: |
          pwd
          ls -a